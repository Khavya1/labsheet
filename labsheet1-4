#include<stdio.h>
int main()
{
int l,b,a,p;
printf("Enter the length and breadth of rectangle:");
scanf("%d%d",&l,&b);
a=l*b;
printf("The area is:%d\n",a);
p=2*(l+b);
printf("the perimeter is:%d\n",p);
}
